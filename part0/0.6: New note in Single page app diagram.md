```mermaid

sequenceDiagram
    participant html(browser)
    participant spa.js(browser)
    participant server

    html(browser)->>spa.js(browser): notes_from event handler activated
    activate spa.js(browser)
    spa.js(browser)-->>html(browser): redrawNotes() called
    spa.js(browser)-->>server: https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note right of html(browser): JSON payload sent to server
    deactivate spa.js(browser)

    activate server
    server-->>html(browser) HTTP status code 201
    deactivate server
    
    
```
